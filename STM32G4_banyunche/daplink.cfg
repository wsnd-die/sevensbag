# ===================================================
# OpenOCD config for STM32G491 via DAPLink (CMSIS-DAP)
# ===================================================

# Use CMSIS-DAP (DAPLink) interface
source [find interface/cmsis-dap.cfg]

# Use SWD transport (DAPLink supports SWD)
transport select swd

# Lower speed for Horco/clone DAPLink stability
# If using genuine DAPLink (V208 firmware), 2000 is fine
# If using Horco firmware, use 500 to avoid disconnection
adapter speed 500

# Target: STM32G4xx family (covers STM32G491CETx)
# This script handles: flash banks, reset config, debug settings
source [find target/stm32g4x.cfg]
